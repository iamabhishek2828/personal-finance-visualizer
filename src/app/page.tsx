import React from 'react';
import Dashboard from '../components/Dashboard';

const HomePage = () => {
    return (
        <main>
            <Dashboard />
        </main>
    );
};

export default HomePage;